"""Skeleton migration utilities."""

from __future__ import annotations

from typing import Iterable


def apply_migrations(migrations: Iterable[str]) -> None:
    """Apply the provided migrations to the persistence layer."""

    for _ in migrations:
        continue
