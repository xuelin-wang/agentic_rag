"""Skeleton export helpers for generated datasets."""

from __future__ import annotations

from pathlib import Path
from typing import Iterable


def export_dataset(rows: Iterable[str], destination: Path) -> None:
    """Write generated rows to the destination path."""

    _ = rows
    _ = destination
